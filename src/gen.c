#include <stdio.h>
#include <assert.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <assert.h>
#include <search.h>

#include "default_types.h"
#include "vector.h"
#include "stack.h"
#include "list.h"
#include "queue.h"

int main() {
    // vector_t * vec = malloc(sizeof(vector_t));
    // vector_init( vec, sizeof(int), NULL, NULL);
    // for(int i = 0; i < 10; i++)
    //     vector_push_back(vec, &i);

    // while(!vector_isEmpty(vec)){
    //     int val;
    //     vector_pop_back(vec, &val);
    //     printf("%i ,", val);
    // }
    // vector_dispose(vec);
    // free(vec);
    
    // stack_t * st = malloc(sizeof(stack_t));
    // stack_init(st, sizeof(int), NULL, NULL);

    // for(int i = 0; i < 10; i++) {
    //     stack_push(st, &i);
    //     // int val;
    //     // stack_peek(st,&val);
    //     // printf("%i ,",val);
    // }

    // while(!stack_isEmpty(st)) {
    //     int val;
    //     stack_pop(st, &val);
    //     printf("%i ,", val);
    // }

    // stack_dispose(st);
    // free(st);

    // list_t * ls = malloc(sizeof(list_t));
    // list_init(ls, sizeof(int), NULL, NULL);

    // for(int i = 0; i < 10; i++)
    //     list_push_front(ls, &i);

    
    // while(!list_isEmpty(ls)) {
    //     int val;
    //     list_pop_front(ls, &val);
    //     printf("%i, ", val);
    // }

    // for(int i = 0; i < 10; i++)
    //     list_push_back(ls, &i);

    // while(!list_isEmpty(ls)) {
    //     int val;
    //     list_pop_back(ls, &val);
    //     printf("%i, ", val);
    // }

    // list_dispose(ls);
    // free(ls);


    return 0;
}